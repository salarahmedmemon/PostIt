<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Data Association Mini Project</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="min-h-screen bg-zinc-900 text-white flex items-center justify-center p-4">
      <div class="w-full max-w-md bg-zinc-800 rounded-lg p-6 shadow-lg">
        <h1 class="text-3xl font-bold mb-6 text-center">Create Account</h1>
        
        <% if (typeof error !== 'undefined') { %>
          <div id="toast" class="fixed top-4 right-4 bg-red-500 text-white px-6 py-3 rounded-lg shadow-lg transition-opacity duration-500">
            <%= error %>
          </div>
          <script>
            setTimeout(() => {
              const toast = document.getElementById("toast");
              if (toast) toast.style.opacity = "0";
            }, 3000);
          </script>
        <% } %>

        
        <form action="/register" method="post" class="space-y-4">
          <input
            class="w-full px-4 py-2 rounded-md outline-none bg-transparent border-2 border-zinc-700 placeholder-zinc-400 focus:border-blue-500"
            type="text"
            placeholder="Name"
            name="name"
            required
          />
          <input
            class="w-full px-4 py-2 rounded-md outline-none bg-transparent border-2 border-zinc-700 placeholder-zinc-400 focus:border-blue-500"
            type="text"
            placeholder="Username"
            name="username"
            required
          />
          <input
            class="w-full px-4 py-2 rounded-md outline-none bg-transparent border-2 border-zinc-700 placeholder-zinc-400 focus:border-blue-500"
            type="number"
            placeholder="Age"
            name="age"
            required
          />
          <input
            class="w-full px-4 py-2 rounded-md outline-none bg-transparent border-2 border-zinc-700 placeholder-zinc-400 focus:border-blue-500"
            type="email"
            placeholder="Email"
            name="email"
            required
          />
          <input
            class="w-full px-4 py-2 rounded-md outline-none bg-transparent border-2 border-zinc-700 placeholder-zinc-400 focus:border-blue-500"
            type="password"
            placeholder="Password"
            name="password"
            required
          />
          <input
            class="w-full px-4 py-2 bg-blue-600 hover:bg-blue-700 transition-colors rounded-md font-semibold cursor-pointer"
            type="submit"
            value="Create Account"
          />
        </form>

        <a
          class="w-full mt-3 inline-block text-center px-4 py-2 bg-blue-600 hover:bg-blue-700 transition-colors rounded-md font-semibold cursor-pointer"
          href="/login">Login Account</a>

      </div>
    </div>
  </body>
</html>